import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Migration3-5',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
